{
	"name": "dummyserver",
	"version": "1.0.0",
	"description": "",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"prebuild": "rm -rf dist && rm -rf logs",
		"build": "tsc",
		"start": "npm run build && node dist/server.js"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.20.2",
		"express": "^4.18.1",
		"ts-node": "^10.9.1",
		"typescript": "^5.0.4",
		"winston": "^3.8.2"
	},
	"devDependencies": {
		"@types/node": "^18.16.3"
	}
}
